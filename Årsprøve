var arr = [0 ,1 ,1 ,1 ,2 ,3 ,3];
var count = {};

for (var i = 0; i < arr.length; i++) {
  var num = arr[i];
  count[num] = count[num] ? count[num] + 1 : 1;
}

console.log(count[0], count[1], count[2], count[3]);
